<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Драйвер очереди по умолчанию
    |--------------------------------------------------------------------------
    |
    | Определяет, какой драйвер используется для обработки задач.
    | Доступные значения: 'sync', 'database'.
    |
    | sync     — задачи выполняются синхронно в текущем процессе.
    | database — задачи записываются в таблицу jobs и обрабатываются воркером.
    |
    */

    'default' => env('QUEUE_DRIVER', 'sync'),

    /*
    |--------------------------------------------------------------------------
    | Дефолтные настройки задач
    |--------------------------------------------------------------------------
    |
    | Используются когда задача не переопределяет свойства timeout/tries/backoff.
    |
    */

    'timeout' => 60,
    'tries'   => 3,
    'backoff'  => [5, 30, 120],

    /*
    |--------------------------------------------------------------------------
    | Очереди и воркеры
    |--------------------------------------------------------------------------
    |
    | Каждая очередь определяет количество параллельных воркеров.
    | queue:start читает этот конфиг и запускает нужное количество процессов.
    |
    */

    'queues' => [
        'default' => ['workers' => 2],
        'emails'  => ['workers' => 1],
    ],

];
