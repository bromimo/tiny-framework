<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Драйвер кеша по умолчанию
    |--------------------------------------------------------------------------
    |
    | Определяет, какое хранилище использовать для кеширования.
    | Доступные значения: 'array', 'file', 'memcached'.
    |
    | array     — хранение в памяти, данные живут только в рамках запроса.
    | file      — персистентный кеш в файловой системе.
    | memcached — персистентный кеш в Memcached, быстрее файлового.
    |
    */

    'default' => env('CACHE_DRIVER', 'array'),

    /*
    |--------------------------------------------------------------------------
    | Настройки драйверов
    |--------------------------------------------------------------------------
    */

    'drivers' => [

        'array' => [],

        'file' => [
            'path' => __DIR__ . '/../storage/cache',
        ],

        'memcached' => [
            'host' => env('MEMCACHED_HOST', '127.0.0.1'),
            'port' => (int) env('MEMCACHED_PORT', 11211),
        ],

    ],

];
