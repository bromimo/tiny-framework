<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Канал логирования по умолчанию
    |--------------------------------------------------------------------------
    |
    | Определяет, какой канал из секции 'channels' использовать по умолчанию.
    | Можно переопределить через переменную окружения LOG_CHANNEL.
    |
    */

    'default' => env('LOG_CHANNEL', 'single'),

    /*
    |--------------------------------------------------------------------------
    | Каналы логирования
    |--------------------------------------------------------------------------
    |
    | Каждый канал описывает способ и место записи логов.
    |
    | Доступные драйверы:
    |   single — один файл с ротацией по размеру.
    |            max_size  — максимальный размер файла в байтах (по умолчанию 10 MB).
    |            max_files — сколько архивных файлов хранить: app.1.log, app.2.log, ...
    |   daily  — отдельный файл на каждый день; суффикс даты добавляется
    |             автоматически: app-2026-03-12.log.
    |            days — сколько дней хранить архивные файлы.
    |
    */

    'channels' => [

        'single' => [
            'driver'    => 'single',
            'path'      => storage_path('logs/app.log'),
            'max_size'  => 10 * 1024 * 1024, // 10 MB
            'max_files' => 5,
        ],

        'daily' => [
            'driver' => 'daily',
            'path'   => storage_path('logs/app.log'),
            'days'   => 14,
        ],

    ],

];
