<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Параметры подключения к базе данных
    |--------------------------------------------------------------------------
    |
    | Настройки PDO-подключения к MySQL. Все значения можно переопределить
    | через переменные окружения в файле .env.
    |
    */

    'host'     => env('DB_HOST', '127.0.0.1'),
    'port'     => env('DB_PORT', '3306'),
    'database' => env('DB_DATABASE', 'proj1'),
    'username' => env('DB_USERNAME', 'root'),
    'password' => env('DB_PASSWORD', ''),

    /*
    |--------------------------------------------------------------------------
    | Настройки миграций
    |--------------------------------------------------------------------------
    |
    | table — имя служебной таблицы для отслеживания применённых миграций.
    |
    | stub  — имя файла-заготовки относительно директории stubs/.
    |         Можно переопределить флагом --stub=<path> при вызове make:migration.
    |
    */

    'migrations' => [
        'table' => 'migrations',
        'stub'  => 'migrations/migration.stub',
    ],

];
